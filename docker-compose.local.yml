# ============================================
# Local Development Override
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
#
# Adds local build targets (instead of pulling GHCR images) and exposes
# debug ports bound to loopback only. Not used in production.
# ============================================

services:
  api:
    image: vooglaadija-api:local
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    environment:
      # Local dev runs over plain HTTP, so cookies must not require HTTPS.
      COOKIE_SECURE: 'False'
    ports:
      - '127.0.0.1:8000:8000'

  worker:
    image: vooglaadija-api:local
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    environment:
      COOKIE_SECURE: 'False'

  celery-beat:
    image: vooglaadija-api:local
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    environment:
      COOKIE_SECURE: 'False'

  flower:
    image: vooglaadija-api:local
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    ports:
      - '127.0.0.1:5555:5555'
    environment:
      COOKIE_SECURE: 'False'
      FLOWER_BASIC_AUTH: 'admin:admin'

  db:
    ports:
      - '127.0.0.1:5432:5432'

  redis:
    ports:
      - '127.0.0.1:6380:6379'

  otel-collector:
    ports:
      - '127.0.0.1:4317:4317'
      - '127.0.0.1:4318:4318'
